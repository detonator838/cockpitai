const AuditLog = () => (
  <div>
    <h1 className="text-3xl font-bold tracking-tight text-foreground">Audit Log</h1>
    <p className="mt-2 text-muted-foreground">Track all agent activities and system events.</p>
  </div>
);
export default AuditLog;
